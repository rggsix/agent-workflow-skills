# 编写规则

1. SKILL.md 必须简洁：控制在短说明 + 引用列表。
2. SKILL.md 不写完整流程细节，不堆例子，不写长篇背景。
3. 详细能力说明必须拆到 `references/*.md`。
4. 可执行逻辑放 `scripts/`，不要把脚本内容粘在 SKILL.md。
5. 产物模板、素材、样板文件放 `assets/`。
6. 一条规则只在一个文件维护，避免重复与冲突。

## 禁止项

- 把全部规则、示例、流程都塞进 SKILL.md。
- 在 SKILL.md 写“多层嵌套文档手册”。
- references 没有被 SKILL.md 显式引用。
