# 规则

一句话定位：定义规划的最小产出与边界。

## 必须

1. 只做规划，不执行修改。
2. 产出必须是 spec-lite（可执行步骤）。
3. 规划结果必须写入 `coding-workflow` 的 `plan.md`。
4. 规划包含最小观察：目标位置 + 约束要点。
5. `plan.md` 必须包含“人类输入摘要”和“AI返述确认”。

## 禁止

- 写长篇背景解释。
- 产出不可执行的抽象描述。
