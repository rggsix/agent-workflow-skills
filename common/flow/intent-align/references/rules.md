# 规则

一句话定位：规定意图返述的输出格式与触发确认的门槛。

## 输出格式

```text
✅ 意图对齐
🎯 目标：...
🛠️ 动作：...
🚫 边界：...
❓ 不确定（可选）：...
```

说明：仅填写必要内容，不写背景解释。

## 执行原则

1. 默认只返述，不阻断执行。
2. 若存在“关键不确定 + 高风险”，必须触发 human-in-loop 询问。
3. 若只是轻微不确定，写在“不确定（可选）”并继续。
4. 每次最多提出 1-2 个问题，避免打断。
5. 唯一产出是“意图对齐块”，不执行任何修改。
6. 返述必须清晰、完整、简洁易懂、明确。
