# 规则

1. 项目根目录 `.ai-workspace/` 是唯一官方状态域。
2. 所有持久化写入必须按 `assets/workspace.yaml` 的映射执行。
3. `workspace.yaml` 默认只允许人类修改；AI 只有在“人类明确授权”时才可修改。
4. 路径映射键就是 skill 名；每个 skill 只能用自己的键查路径。
5. skill 名未出现在映射中时，必须直接报错并请求人类处理。
6. 禁止创建平行结构、重复真相或私有状态目录。
