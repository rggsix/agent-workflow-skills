# 提交信息规范

一句话定位：定义 commit message 的固定格式与文案约束。

## 格式

`<type>: <description>`

## Type

`feat` / `fix` / `refactor` / `docs` / `style` / `perf` / `test` / `chore`

## 描述规则

1. 使用中文。
2. 使用现在时（如：修复 / 增加 / 重构）。
3. 不超过 72 字符。
4. 不解释背景。
5. 不输出额外文本。
6. 若变更仅涉及单一类或方法，`description` 必须点名该类或方法。
