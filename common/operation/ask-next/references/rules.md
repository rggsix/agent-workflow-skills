# 规则

一句话定位：定义 ask-next 的只读边界和最小输出。

## 必须

1. 只读取 `.ai-workspace/todo/active.md`。
2. 按优先级顺序给建议：`P0` -> `P1` -> `P2`。
3. 默认输出 1-3 条建议，不输出全量清单。
4. 若无待办，返回“当前无待办”并给 1 条可选建议。

## 禁止

- 不修改 todo。
- 不创建新任务。
- 不归档任务。
